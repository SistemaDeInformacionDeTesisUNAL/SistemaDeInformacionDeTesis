<%= form_for(tag_investigation_group) do |f| %>
  <% if tag_investigation_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag_investigation_group.errors.count, "error") %> prohibited this tag_investigation_group from being saved:</h2>

      <ul>
      <% tag_investigation_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %>
    <%= f.text_field :tag_id %>
  </div>

  <div class="field">
    <%= f.label :investigationGroup_id %>
    <%= f.text_field :investigationGroup_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
