<%= stylesheet_link_tag 'css/group_table' %>

<h1 style="color: #95b33b; text-align: center; margin-top: 5%">New Contribution</h1>

<% @event.investigation_group_id = @contribution_group.id %>
<%= render 'form', event: @event %>

<%= link_to 'Group Events', eventsGroup_investigation_groups_path(@contribution_group), :class=>"btn btn1"%>
