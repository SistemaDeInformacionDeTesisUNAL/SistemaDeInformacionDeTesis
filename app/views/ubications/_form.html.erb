<%= form_for(ubication) do |f| %>
  <% if ubication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ubication.errors.count, "error") %> prohibited this ubication from being saved:</h2>

      <ul>
      <% ubication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link %>
    <%= f.text_area :link %>
  </div>

  <div class="field">
    <%= f.label :contribution_id %>
    <%= f.text_field :contribution_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
