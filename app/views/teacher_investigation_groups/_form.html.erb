<%= form_for(teacher_investigation_group) do |f| %>
  <% if teacher_investigation_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teacher_investigation_group.errors.count, "error") %> prohibited this teacher_investigation_group from being saved:</h2>

      <ul>
      <% teacher_investigation_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :teacher_id %>
    <%= f.text_field :teacher_id %>
  </div>

  <div class="field">
    <%= f.label :investigation_group_id %>
    <%= f.text_field :investigation_group_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
