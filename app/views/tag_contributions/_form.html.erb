<%= form_for(tag_contribution) do |f| %>
  <% if tag_contribution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag_contribution.errors.count, "error") %> prohibited this tag_contribution from being saved:</h2>

      <ul>
      <% tag_contribution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tag_id %>
    <%= f.text_field :tag_id %>
  </div>

  <div class="field">
    <%= f.label :contribution_id %>
    <%= f.text_field :contribution_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
