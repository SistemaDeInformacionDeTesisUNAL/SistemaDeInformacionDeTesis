<%= form_for(event_teacher) do |f| %>
  <% if event_teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_teacher.errors.count, "error") %> prohibited this event_teacher from being saved:</h2>

      <ul>
      <% event_teacher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %>
    <%= f.text_field :event_id %>
  </div>

  <div class="field">
    <%= f.label :teacher_id %>
    <%= f.text_field :teacher_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
