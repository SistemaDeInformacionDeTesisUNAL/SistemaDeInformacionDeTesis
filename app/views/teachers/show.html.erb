<p id="notice"><%= notice %></p>

<%= stylesheet_link_tag 'css/profile' %>
<div class="container">
  <div class="col-md-offset-2 col-md-3", style="margin-top: 2%">
    <%= image_tag(@teacher.image_url.to_s,:class=>"img-circle img-responsive") %>
  </div>
  <div class="col-md-4">
    <div class="profile-sidebar">
      <div class="profile-usertitle">
        <div class="profile-usertitle-name">
          <p> <%= @teacher.first_name %> <%= @teacher.last_name %> </p>
        </div>
        <div class="profile-usertitle-job">
          <p>TEACHER</p>
        </div>
        <div class="profile-usertitle-job">
          <strong style="color: black;">Username:</strong>
          <%= @teacher.username %>
        </div>
        <div class="profile-usertitle-job">
          <strong style="color: black;">Email:</strong>
          <%= @teacher.email %>
        </div>
        <div class="profile-usertitle-job">
          <strong style="color: black;">Investigation groups:</strong>
          <%@teacher.investigation_groups.each do |groupe|%>
          <%= link_to groupe.name, :controller => "investigation_groups", :action => "show", :id => groupe.id%>
          <%  if groupe != @teacher.investigation_groups.last %>,
          <%end
        end %>
      </div>
      <div class="col-md-8 col-md-offset-2">
        <table class=" table table-bordered dataTables_wrapper form-inline dt-bootstrap nowrap  no-footer">
          <thead>
            <tr role="row">
            <th class="ui-state-default">Contibutions</th>
          </tr>
          </thead>
            <tbody>
            <tr>
            <%@teacher.contributions.each do |contribution|%>
              <td >
                <%= link_to contribution.name, :controller => "contributions", :action => "show", :id => contribution.id%>
              </td>
            </tr>
            <%end%>
          </tbody>
        </table>
      </div>
      <br>
      <div class="profile-userbuttons">
        <table style="float: none; margin: 0 auto;">
          <tr>
              <% if teacher_signed_in? %>
              <% if current_teacher.id == @teacher.id %>
                <th><%= link_to 'Edit', edit_teacher_path(@teacher),  :class=>"ui-button ui-corner-all ui-widget" %>  </th>
              <% end %>
              <% end %>
              <th><%= link_to 'Teachers', teachers_path,  :class=>"ui-button ui-corner-all ui-widget" %>  </th>
              <% if teacher_signed_in? %>
              <% if current_teacher.id == @teacher.id %>
              <th><%= link_to('Log Out', destroy_teacher_session_path, :method => :delete,  :class=>"ui-button ui-corner-all ui-widget") %>  </th>
              <% end %>
              <% end %>
          </tr>
        </table>
      </div>
    </div>
  </div>
</div>
</div>
