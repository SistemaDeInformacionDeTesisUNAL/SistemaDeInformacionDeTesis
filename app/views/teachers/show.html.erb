<p id="notice"><%= notice %></p>

<%= stylesheet_link_tag 'css/profile' %>
<div class="container">

  <div class="col-md-4 col-md-offset-4">
    <div class="profile-sidebar">
      <!--div class="profile-userpic">
      <img src="https://pbs.twimg.com/profile_images/828436452748951554/0Zi7F0Kc.jpg" class="img-responsive" alt="">
    </div-->
    <div class="profile-usertitle">
      <div class="profile-usertitle-name">
        <p> <%= @teacher.first_name %> <%= @teacher.last_name %> </p>
      </div>
      <div class="profile-usertitle-job">
        <p>TEACHER</p>
      </div>
      <div class="profile-usertitle-job">
        <strong style="color: black;">Username:</strong>
        <%= @teacher.username %>
      </div>
      <div class="profile-usertitle-job">
        <strong style="color: black;">Email:</strong>
        <%= @teacher.email %>
      </div>
      <%if !@groupsOwner.empty? || !@groups.empty?%>
        <strong style="color: black;">Investigation groups:</strong>
        <% if !@groupsOwner.empty?%>
          <div class="profile-usertitle-job">
            <strong style="color: black;">Owner:</strong>
            <%@groupsOwner.each do |group|%>
              <%= link_to group.name, :controller => "investigation_groups", :action => "show", :id => group.id%>
              <%if group != @groupsOwner.last %>,
              <%end%>
            <%end%>
          </div>
        <%end%>
        <%if !@groups.empty?%>
          <div class="profile-usertitle-job">
            <strong style="color: black;">Menber:</strong>
            <%@groups.each do |groupe|%>
              <%= link_to groupe.name, :controller => "investigation_groups", :action => "show", :id => groupe.id%>
              <%  if groupe != @groups.last %>,
              <%end%>
            <%end%>
          </div>
        <%end%>
      <%end%>
      <%if !@teacher.contributions.empty?%>
        <strong style="color: black;">Contributions:</strong>
        <div class="profile-usertitle-job">
          <%@teacher.contributions.each do |contribution|%>
            <%= link_to contribution.name, :controller => "contributions", :action => "show", :id => contribution.id%>
            <%  if contribution != @teacher.contributions.last %>,
            <%end%>
          <%end%>
        </div>
      <%end%>
    <div class="profile-userbuttons">
      <% if teacher_signed_in? %>
        <% if current_teacher.id == @teacher.id %>
          <%= link_to 'Edit', edit_teacher_path(@teacher), :class=>"btn btn-success btn-sm" %>
        <% end %>
      <% end %>
      <%= link_to 'Back', teachers_path, :class=>"btn btn-success btn-sm" %>
      <% if teacher_signed_in? %>
        <% if current_teacher.id == @teacher.id %>
          <%= link_to('Log out', destroy_teacher_session_path, :method => :delete, :class=>"btn btn-success btn-sm") %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
</div>

</div>
